class Solution(object):
    def reverseString(self, s):
        a=0
        b=len(s)-1
        reverse=""
        while(a<b):
            s[a],s[b]=s[b],s[a]
            a=a+1
            b=b-1
        for i in s:
            reverse=reverse+i
        return reverse
